apiVersion: secretops.io/v1alpha1
kind: SecretRotation
metadata:
  name: test-rotation
  namespace: default
spec:
  secretRef:
    name: my-db-secret
    namespace: default
  rotationSchedule: "* * * * *"
  rotationStrategy: external
